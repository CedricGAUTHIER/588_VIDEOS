import React from 'react';

import './Search.scss';

function Search({setPage}) {
  setPage('recherche');
  return (
    <div className="search">
       SEARCH
    </div>
  );
}

export default Search;
